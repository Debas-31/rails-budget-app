<div class="flex flex-col items-center mb-20">
<%= render "header" %>
  <h3 class="text-sky-400 text-xl font-bold mt-4 mb-6">Add a new category</h3>
  <%= form_with url: groups_path, scope: :group do |form| %>
    <div class="mt-2">
      <%= form.label :Name %> <br>
      <%= form.text_field :name, class: 'mt-2 w-56 p-1 rounded', require: true %>
    </div>

    <div class="mt-2">
      <%= form.label :Name %> <br>
      <%= form.text_field :name, class: 'mt-2 w-56 p-1 rounded', require: true %>
    </div>
    
    <%= form_with.submit "Submit", class: "bg-sky-400 p-2 rounded text-slate-50 mt-4"%>
    <%= link_to 'Back', groups_path, class: "bg-sky-400 rounded text-slate-50 mt-4 ml-20 px-4 py-3" %>
  <% end %>
</div>